float4 main(uniform float alpha_value,
			uniform float time
) : COLOR
{
   float4 color;
   color.x = 0.0;
   color.y = 0.55;
   color.z = 1.0;
   color.w = alpha_value * (sin(time * 8.0));
   return color;
}
